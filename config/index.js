/***********************************
 * Module dependencies.
 ************************************/
var dotenv = require("dotenv");
var config = require("config");

dotenv.load();

module.exports = config;
